server:
  port: 9090

spring:
  application:
    name: CIPES-AUTH-SERVICE

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/